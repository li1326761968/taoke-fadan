# GitHub 仓库初始化（只需执行一次）
# 把下面的 YOUR_USERNAME 换成你的 GitHub 用户名

git init
git add .
git commit -m "v1.0.0 初始版本"
git branch -M main
git remote add origin https://github.com/li1326761968/taoke-fadan.git
git push -u origin main

# 推送成功后，以后每次更新只需要双击「一键发布新版本.bat」
